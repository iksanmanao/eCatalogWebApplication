@model IEnumerable<eCatalogWebApplication.Models.Catalog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameCatalog)
            </th>
         @*   <th>
                @Html.DisplayNameFor(model => model.SubCatalog)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameCatalog)
            </td>
         @*   <td>
                @Html.DisplayFor(modelItem => item.SubCatalog)
            </td>*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDCatalog">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDCatalog">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDCatalog">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
